# XpERP AI Admin

정적 HTML + CSS + JS 기반 관리자 UI입니다.

## 실행

1. `index.html`을 더블클릭합니다.
2. 브라우저에서 바로 사용합니다.

## 구조

- `index.html`: 단일 실행 파일
- `docs/`: 기능 문서
- `guidelines/`: 작업 가이드

## 주의

- `package.json` 없음
- `node_modules` 없음
- 기능과 디자인은 기존 배포본 기준으로 유지됩니다.
